// metodo 2 DIVIDINDO
const primeiro = [1,2,3];
const segundo = [4,5,6];

const combinado = primeiro.concat(segundo);
console.log(combinado);

const cortando = combinado.slice(1);
console.log(cortando);

//aqui ele divide e começa apartir do numero que foi posto como indice e vai até o final do array
